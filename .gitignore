node_modules/
dist/
*.log

# 브라우저 프로필(쿠키·로그인 정보가 들어있음) — 절대 커밋 금지
.profile/
profiles/

# 조사 결과물(사이트 내부 구조가 들어있음)
inspection/

# 로그인 정보가 든 파일 — 절대 커밋 금지
*.session.json

# 1회용 스크립트 빌드 결과. 소스(scripts/*.ts)만 남깁니다.
scripts/.build/
